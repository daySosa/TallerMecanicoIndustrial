<Window x:Class="Órdenes_de_Trabajo.AgregarRepuesto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Órdenes_de_Trabajo"
        mc:Ignorable="d"
        Title="Agregar Repuesto" Height="375" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Opacity="0"
        Loaded="Window_Loaded"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <DropShadowEffect x:Key="WinShadow" Color="#000000" BlurRadius="40" ShadowDepth="0" Opacity="0.75"/>

        <Style x:Key="EtiquetaCampo" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#353a58"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="CajaCampo" TargetType="Border">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Height" Value="38"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border CornerRadius="14" Effect="{StaticResource WinShadow}">
            <Border CornerRadius="14" ClipToBounds="True" Background="#0f1117">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#13161f" Height="54"
                            BorderBrush="#1e2235" BorderThickness="0,0,0,1">
                        <Grid Margin="20,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Agregar Repuesto" Foreground="White"
                                           FontSize="14" FontWeight="Bold"/>
                                <TextBlock Text="Selecciona el producto y la cantidad"
                                           Foreground="#505880" FontSize="11"/>
                            </StackPanel>
                            <Button Grid.Column="1"
                                    Style="{StaticResource MaterialDesignIconButton}"
                                    Foreground="#505880" Width="34" Height="34"
                                    Click="btnCancelar_Click">
                                <materialDesign:PackIcon Kind="Close" Width="18" Height="18"/>
                            </Button>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Row="1" Margin="20,18,20,20">

                        <TextBlock Text="PRODUCTO" Style="{StaticResource EtiquetaCampo}"/>
                        <Border Background="#1e2235" BorderBrush="#252840" Height="38"
                                CornerRadius="8" BorderThickness="1" Margin="0,0,0,16">
                            <ComboBox x:Name="cmbProducto"
                                      Background="Transparent" Foreground="White"
                                      BorderThickness="0" Padding="10,0"
                                      VerticalContentAlignment="Center"
                                      DisplayMemberPath="Producto_Nombre"
                                      Style="{StaticResource MaterialDesignComboBox}"
                                      SelectionChanged="cmbProducto_SelectionChanged">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Background" Value="#1e2235"/>
                                        <Setter Property="Padding" Value="10,8"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#2e3250"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="#2e3250"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>
                        </Border>

                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="CATEGORÍA" Style="{StaticResource EtiquetaCampo}"/>
                                <Border Style="{StaticResource CajaCampo}"
                                        Background="#13161f" BorderBrush="#1e2235">
                                    <TextBox x:Name="txtCategoria"
                                             Foreground="#505880" Background="Transparent"
                                             BorderThickness="0" IsReadOnly="True"
                                             VerticalContentAlignment="Center" Padding="10,0"
                                             Text="-" FontSize="12"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="PRECIO UNIT." Style="{StaticResource EtiquetaCampo}"/>
                                <Border Style="{StaticResource CajaCampo}"
                                        Background="#13161f" BorderBrush="#1e2235">
                                    <TextBox x:Name="txtPrecioUnitario"
                                             Foreground="#505880" Background="Transparent"
                                             BorderThickness="0" IsReadOnly="True"
                                             VerticalContentAlignment="Center" Padding="10,0"
                                             Text="L 0.00" FontSize="12"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="4">
                                <TextBlock Text="STOCK" Style="{StaticResource EtiquetaCampo}"/>
                                <Border x:Name="borderStock" Style="{StaticResource CajaCampo}"
                                        Background="#13161f" BorderBrush="#1e2235">
                                    <TextBox x:Name="txtStock"
                                             Foreground="#505880" Background="Transparent"
                                             BorderThickness="0" IsReadOnly="True"
                                             VerticalContentAlignment="Center" Padding="10,0"
                                             Text="0" FontSize="12"/>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="CANTIDAD" Style="{StaticResource EtiquetaCampo}"/>
                                <Border Style="{StaticResource CajaCampo}"
                                        Background="#1e2235" BorderBrush="#252840">
                                    <TextBox x:Name="txtCantidad"
                                             Foreground="White" Background="Transparent"
                                             BorderThickness="0" Text="1"
                                             CaretBrush="White" MaxLength="4"
                                             VerticalContentAlignment="Center" Padding="10,0"
                                             FontSize="13"
                                             PreviewTextInput="txtCantidad_PreviewTextInput"
                                             TextChanged="txtCantidad_TextChanged"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Grid.ColumnSpan="3">
                                <TextBlock Text="SUBTOTAL" Style="{StaticResource EtiquetaCampo}"/>
                                <Border x:Name="borderSubtotal" Style="{StaticResource CajaCampo}"
                                        Background="#1a1d35" BorderBrush="#4f6ef7">
                                    <TextBox x:Name="txtSubtotal"
                                             Foreground="#4f6ef7" Background="Transparent"
                                             BorderThickness="0" IsReadOnly="True"
                                             VerticalContentAlignment="Center" Padding="10,0"
                                             FontWeight="Bold" Text="L 0.00" FontSize="13"/>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    BorderBrush="#1e2235" Foreground="#505880"
                                    materialDesign:ButtonAssist.CornerRadius="8"
                                    Height="38"
                                    Click="btnCancelar_Click">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <materialDesign:PackIcon Kind="Close" Width="15" Height="15"
                                                             VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBlock Text="Cancelar" VerticalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Button>

                            <Button Grid.Column="2"
                                    materialDesign:ButtonAssist.CornerRadius="8"
                                    materialDesign:ElevationAssist.Elevation="Dp0"
                                    Height="38"
                                    Click="btnAgregar_Click">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                        <Setter Property="Background" Value="#4f6ef7"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderBrush" Value="#4f6ef7"/>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <materialDesign:PackIcon Kind="Plus" Width="15" Height="15"
                                                             VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBlock Text="Agregar Repuesto" VerticalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>